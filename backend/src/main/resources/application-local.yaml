spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/plavor_local
    username: plavor
    password: plavor_password

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

management:
  endpoint:
    health:
      show-details: always

app:
  jwt:
    secret: plavor-local-jwt-secret-key-for-development-only-change-me-2026
