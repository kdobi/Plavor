spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/plavor_test
    username: plavor
    password: plavor_password

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

management:
  endpoint:
    health:
      show-details: always
