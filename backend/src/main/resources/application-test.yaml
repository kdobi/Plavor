spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/plavor_test
    username: plavor
    password: plavor_password

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

management:
  endpoint:
    health:
      show-details: always

app:
  jwt:
    secret: plavor-test-jwt-secret-key-for-automated-tests-only-2026
  kakao:
    rest-api-key: test-kakao-rest-api-key
    client-secret: test-kakao-client-secret
    redirect-uri: http://127.0.0.1:5173/auth/kakao/callback
