POSTGRES_DB=plavor
POSTGRES_USER=plavor
POSTGRES_PASSWORD=change-this-to-a-long-random-password

# Use 8080 for a first smoke test: http://SERVER_IP:8080/actuator/health
# If only port 80 is forwarded, set this to 80 instead.
BACKEND_PORT=8080
